import { PagePlaceholder } from "@/components/ui/page-placeholder";

export default function QuotationsPage() {
  return (
    <PagePlaceholder
      eyebrow="Quotations"
      title="Quotation intake and review"
      description="Upload supplier documents, verify extracted values, and keep every quotation tied to its procurement request."
      icon="quotation"
      phase="Phases 7–8"
    />
  );
}
