import { PagePlaceholder } from "@/components/ui/page-placeholder";

export default function QuotationsPage() {
  return (
    <PagePlaceholder
      eyebrow="Quotations"
      title="Quotation intake and review"
      description="Import local supplier files, verify structured values, and keep every quotation tied to its issued RFQ without requiring permanent file storage."
      icon="quotation"
      phase="Phases 7–8"
    />
  );
}
